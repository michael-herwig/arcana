# Retro: 2026-09-23 — import handover_dogfood_findings.md

Inputs: 295 entries (0 self, 283 trajectory, 12 seed), 0 skipped, 13 rows touched — as of 2026-09-23T17:25:20Z
Degraded: objective channel — transcript shape: 7 file(s) with unrecognised records or no tool pairs (record versions: 2.1.263)

> Note (2026-09-23, plan_retro WP 7): this report predates the E5 and E7 fixes
> and E9–E13 (plan_retro execution log) — the `Degraded:` line uses the pre-E5
> wording (a tool-free file counted), `Inputs:` lacks the E7 `ignored` count.
> Not regenerated: its entries are consumed and the later fixes
> change no proposal.

## Harness

### P-1 — Close the heavy-slot lock descriptor for descendants (F-001)

- Target: hex/hex-core/references/resources.md#3-the-heavy-semaphore
- Change: none this run — the fix already ships: property 2 closes `8>&-` and `9>&-` on the command so a daemonizing build tool cannot inherit the slot, `flock -w` bounds every wait, and the recovery names `fuser`/`lsof` on the token. Row closed `fixed`, `by` = that file.
- Rationale: F-001 observed a suite lock held forever by a daemon that inherited fd 9: "The next run blocks on `flock` forever: there is no timeout, no output, and no process anyone would think to look at." The existing idiom removes both the inheritance and the unbounded wait; the finding's auto-evict ask stays unadopted because its own secondary rule says killing the daemon mid-build is unsafe.
- Size: small
- Route: applied (pre-existing fix; no edit this run)
- Ledger: hex-core-flock-fd-inheritance

### P-2 — Never end a turn on an armed Monitor alone (F-002)

- Target: hex/hex-core/references/protocol.md#worker-liveness
- Change: add one rule to the cadence list: an agent that launches a background gate either runs it in the foreground when it is short, or arms its watcher and re-reads the gate log on its next tool round; a watcher is never the only wake path.
- Rationale: F-002: "still idle eight minutes after `=== DONE ===` appeared" — the designed-in watcher failed silently while the orchestrator's poll caught both stalls. Mined trajectory adds 3 entries (80.6 min tool-busy) of foreground `until` poll loops and a refused `sleep`, the cost of waiting without a named rule.
- Size: small
- Route: deferred
- Ledger: hex-core-monitor-not-wake-signal

### P-3 — Process checks print their matches, never a count (F-003)

- Target: hex/hex-core/references/workers.md#universal-worker-protocol
- Change: add a universal rule: a check for a running process prints what matched and excludes its own pid and parent pid; a bare match count is never read as "a process is running".
- Rationale: F-003: "A detector that can match its own invocation returns the same answer in both states." Two hits, both the scan itself, argued for waiting on zero builds.
- Size: small
- Route: deferred
- Ledger: hex-core-self-matching-process-scan

### P-4 — Give the retro cluster step the harness-defect shapes to look for (F-004)

- Target: hex/hex-retro/SKILL.md#flow
- Change: extend step 5 (Cluster) with one sentence naming the shapes that diff-scoped review misses — an instrument correct but unwired, a guard whose red was never shown, a wait that can deadlock, a detector that matches itself, an agent idle while work was available — as cues for matching entries to rows.
- Rationale: F-004 asked for a pass that separates harness from project defects; `hex-retro` now ships that pass and the discriminator ("would this have happened on a different codebase with the same skills?"). What it does not carry is the finding's list of shapes, which "none of them show up in a diff-scoped review".
- Size: small
- Route: deferred
- Ledger: hex-retro-harness-defect-shapes

### P-5 — Name an in-repo evidence home and the converse "absent is not absent" rule (F-005)

- Target: hex/hex-core/references/resources.md#5-per-run-scratch-environment
- Change: add a paragraph: gate logs go to a run-scoped directory inside the repository (gitignored), each ending in greppable exit-code and completion markers; an orchestrator that does not find a log there asks the worker for its path before reasoning around the gap.
- Rationale: F-005: the supervisor declared a log lost while it sat in a private session scratchpad — "Nothing is lost. The logs were in my session scratchpad". The per-run scratch root is outside every checkout by design, so nothing names a shared, durable home for evidence.
- Size: small
- Route: deferred
- Ledger: hex-core-gate-evidence-home

### P-6 — Take a gate's verdict from its log marker, not a terminal 127 (F-006)

- Target: hex/hex-core/references/verify.md#verification
- Change: add a rule: exit 127 at a gate's last step can be an exec failure from an oversized environment (E2BIG) after every check passed — read the verdict from the log's own completion marker; and a git operation that can start the project gate is named before it runs.
- Rationale: F-006: a message-only amend started a full gate that ended "(eval):1: argument list too long: python3" with code 127 after a green run, so the exit code reported failure. The amend-triggers-gate mechanism was never identified; the opacity is the finding.
- Size: small
- Route: deferred
- Ledger: hex-core-exit-127-e2big

### P-7 — Read a status artefact's writer and validator before citing it (F-007)

- Target: hex/hex-core/references/workers/reviewer.md
- Change: add to the reviewer brief: before citing a status artefact (a mark, a stamp, a cache file) as evidence, read the code that writes it and the code that checks it, and state in the same sentence what it does not cover.
- Rationale: F-007: the same verify mark was cited and "corrected three times inside about an hour"; every correction came from reading the writer and the validator, none from reasoning about the field's name.
- Size: small
- Route: deferred
- Ledger: hex-review-evidence-writer-validator

### P-8 — Floor every negative search on a positive control (F-008)

- Target: hex/hex-core/references/workers.md#universal-worker-protocol
- Change: add a universal rule: a claim of "no X exists" is reported only after the same search is shown matching a known instance of each spelling the language admits; a search with zero true positives is reported as inconclusive.
- Rationale: F-008: a regex reported "no executable caller" while the real caller used the Taskfile dispatch form — "the search returned no true positives at all". Same non-discriminating shape as F-003.
- Size: small
- Route: deferred
- Ledger: hex-core-negative-search-control

### P-9 — Prove a red by its reason, and trace what invokes a guard (F-009)

- Target: hex/hex-core/references/workers/tester.md
- Change: add two rules to the tester brief: a demonstrated red asserts on the failure reason, not the exit code, and identifiers handed to an intercepted service are unresolvable at the real one; for a new guard, state what invokes it, with which arguments, and which gate runs it.
- Rationale: F-009: a required red fired on "HTTP status 401" from a missing token, not the absent fixture, and three self-tested instruments were "reachable by nothing" — one refusal "has never been able to red in production".
- Size: small
- Route: deferred
- Ledger: hex-core-red-reason-unwired-guard

### P-10 — Name the supervisor's liveness reading and a one-line ack (F-010)

- Target: hex/hex-core/references/protocol.md#worker-liveness
- Change: add a named reading order for a quiet agent — its beat, then the evidence-dir mtime, then the lock holder, then the process table through `/proc` — and an ack convention: an agent receiving a ruling names it in its next message.
- Rationale: F-010: the supervisor rebuilt the same discriminator "four separate times", and a `setsid` gate was invisible to `ps` while alive in `/proc`. The heartbeat file already carries `blocked_on`; the procedure for reading a quiet agent and the message ack are still absent.
- Size: small
- Route: deferred
- Ledger: hex-core-liveness-message-ack

### P-11 — Never pull a worker on "idle and quiet" alone (F-011)

- Target: hex/hex-core/references/protocol.md#worker-liveness
- Change: add one rule: "idle with no live process" is not a stall until the agent's own beat says otherwise — a `done` beat or an unexpired `expect_next_s` means wait; with no beat, wait at least the gap since its last report before pulling.
- Rationale: F-011: a worker pulled for stalling had "in fact just finished: its report was in flight". The beat's `done` state already is the ack channel the finding asks for; the pull rule does not yet consult it.
- Size: small
- Route: deferred
- Ledger: hex-core-idle-stall-ambiguity

### P-12 — Require restore integrity from any mutation helper (F-012)

- Target: hex/hex-core/references/workers/tester.md
- Change: add a rule for mutation probes: back up per edit, not per run; hash each file before the mutation and after the restore and fail on mismatch; fail when the mutated text is absent before the run or not in the form the reader keys on.
- Rationale: F-012: one backup per run lost the first of two edits, and the residue was "caught by the next compile — not by the harness". A green mutation is ambiguous between a weak guard and a mutation that missed.
- Size: small
- Route: deferred
- Ledger: hex-core-mutation-harness-integrity

## Project

### P-13 — Name the search binary agents must use under the rtk hook

- Target: CLAUDE.md#verification
- Change: add one line: shell searches use `/usr/bin/grep` (the rtk hook rewrites bare `grep`), and a `grep` exit 1 means no match, not a failure.
- Rationale: mined, not seeded: 47 trajectory entries of repeated failed `grep` calls across sessions (tell: "tool_result is_error in the transcript"); every orchestrator brief in this repo restates the `/usr/bin/grep` workaround by hand. Cause per call is not isolated — no-match exit 1 also counts as an error.
- Size: small
- Route: deferred
- Ledger: project-grep-failures

## Prunes considered

- hex/hex-core/references/resources.md#3-the-heavy-semaphore — kept: F-001 is the incident its descriptor-closing property exists for; the property is load-bearing, not decoration.
- hex/hex-core/references/protocol.md#worker-liveness — kept: F-010 and F-011 argue for more of the heartbeat contract (P-10, P-11), not less.

## Upstream drafts

none — every row's artifact has its source directory in this repo (local).

## Deferred

- [ ] Never end a turn on an armed Monitor alone — P-2
- [ ] Process checks print their matches, never a count — P-3
- [ ] Give the retro cluster step the harness-defect shapes to look for — P-4
- [ ] Name an in-repo evidence home and the converse "absent is not absent" rule — P-5
- [ ] Take a gate's verdict from its log marker, not a terminal 127 — P-6
- [ ] Read a status artefact's writer and validator before citing it — P-7
- [ ] Floor every negative search on a positive control — P-8
- [ ] Prove a red by its reason, and trace what invokes a guard — P-9
- [ ] Name the supervisor's liveness reading and a one-line ack — P-10
- [ ] Never pull a worker on "idle and quiet" alone — P-11
- [ ] Require restore integrity from any mutation helper — P-12
- [ ] Name the search binary agents must use under the rtk hook — P-13

## Ledger

| id | status | occ | folds | cost_min | big |
|---|---|---|---|---|---|
| hex-core-exit-127-e2big | deferred | 1 | 1 | 0 | no |
| hex-core-flock-fd-inheritance | fixed | 1 | 1 | 0 | no |
| hex-core-gate-evidence-home | deferred | 1 | 1 | 0 | no |
| hex-core-idle-stall-ambiguity | deferred | 1 | 1 | 0 | no |
| hex-core-liveness-message-ack | deferred | 1 | 1 | 0 | no |
| hex-core-monitor-not-wake-signal | deferred | 4 | 1 | 80.6 | no |
| hex-core-mutation-harness-integrity | deferred | 1 | 1 | 0 | no |
| hex-core-negative-search-control | deferred | 1 | 1 | 0 | no |
| hex-core-red-reason-unwired-guard | deferred | 1 | 1 | 0 | no |
| hex-core-self-matching-process-scan | deferred | 1 | 1 | 0 | no |
| hex-retro-harness-defect-shapes | deferred | 1 | 1 | 0 | no |
| hex-review-evidence-writer-validator | deferred | 1 | 1 | 0 | no |
| project-grep-failures | deferred | 47 | 1 | 0 | no |
